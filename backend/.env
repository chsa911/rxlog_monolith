# MongoDB Atlas or local Mongo
MONGO_URI=mongodb://127.0.0.1:27017/bmarkdb

# API server port
PORT=4000

# Frontends allowed (comma-separated). Leave empty to allow all during dev.
CORS_ORIGIN=http://localhost:5173,http://localhost:19006
